You are working directly inside an existing repository for a classroom-management application used at iD Tech.

This is a completely new session with no prior context. Do not assume anything about the project until you inspect the repository.

The repository is currently small and may primarily contain files such as:

* `README.md`
* `agent.js`
* `app.js`
* `app.cs`
* `index.html`
* `server.js`
* PowerShell scripts for starting, installing, packaging, or managing the agent and server
* Possibly other supporting files

Your job is to understand the existing system, preserve work that is already present, finish partial features, and implement the requested functionality completely.

Do not rebuild the project from scratch unless the repository is genuinely unusable. Do not introduce a new framework, package manager, bundler, database, or large architecture without a concrete need.

## First steps

Before modifying any code:

1. Read `README.md` completely.
2. Inspect every source file and PowerShell script relevant to the agent, server, website, Windows client, messaging, monitoring, and packaging.
3. Run:

   * `git status`
   * `git diff`
   * `git log -5 --oneline`, when Git history is available
4. Identify:

   * What the server does
   * What `agent.js`, `app.js`, and `app.cs` each do
   * How the browser interface communicates with the server
   * How student clients connect
   * Whether `app.cs` is a launcher, watchdog, WinForms/WPF client, packaging wrapper, or another component
   * What work is already partially implemented
   * What startup and packaging scripts already exist
5. Preserve all working behavior and all existing uncommitted changes.

After inspection, provide a short implementation checklist tied to the actual files, then immediately begin implementing it. Do not stop after analysis or planning.

## Dependency constraint

This project should not require npm package installation.

Do not add npm dependencies, `node_modules`, a bundler, or a frontend framework merely to implement fuzzy search. The implementation should be similar to the publically avalible `fuzzysort` library, though.

Create a reusable local fuzzy-search utility that provides behavior comparable to a simple fuzzy finder:

* Case-insensitive matching
* Subsequence matching
* Better scores for consecutive matching characters
* Better scores for word beginnings
* Better scores for exact prefixes
* Stable and deterministic ordering
* A clear “no match” result
* No remote CDN dependency
* No npm install requirement

Keep the algorithm isolated in one reusable function or module so it can be replaced later if necessary.

## Safety and transparency boundaries

This is visible classroom-management software, not covert monitoring software.

Do not implement:

* Hiding the agent from Task Manager
* Disguising it as an unrelated Windows process
* Rootkit behavior
* Undocumented persistence
* Attempts to defeat an administrator who intentionally stops the software
* Mutual process resurrection that ignores an authorized shutdown
* Privilege escalation
* Endpoint-security bypasses
* Collection of keystrokes, passwords, private document contents, or private messages
* An undocumented secret key combination as the only way to stop the software

Transparent behavior is allowed:

* A visible watchdog may restart the main agent after a crash or accidental close.
* The client may launch automatically through a documented startup mechanism.
* An instructor-enforced message may reappear if its display window is accidentally closed while the server still considers the message active.
* An authorized administrator must still be able to stop or uninstall the agent and watchdog.
* Processes and files must use clear iD Tech product names.
* Security-sensitive commands should be logged.

## Feature 1: Location, building, classroom, and class search

Add a fuzzy-search option to the interface used to navigate locations or classes.

The search should cover all relevant available fields, including where present:

* Location name
* Building name
* Classroom name
* Class name
* Room
* Location identifier
* Other useful tile metadata

Requirements:

* Keep the existing tile interface as the primary navigation method.
* Place the search control on the right side of the location/class navigation section.
* Style it as a convenient secondary navigation option.
* Use the local no-dependency fuzzy-search utility.
* Do not use npm.
* Do not require a network connection.

When the user types a query:

* Rank matching tiles by fuzzy score.
* Move matching tiles to the front.
* Preserve a stable original order when scores are tied.
* Visually de-emphasize nonmatching tiles using reduced opacity or a gray treatment.
* Keep nonmatching tiles visible.
* Keep nonmatching tiles clickable.
* Do not delete or fully hide unmatched tiles.
* Clearing the query must restore the original ordering and styling.

Autocomplete behavior:

* Show ranked suggestions where appropriate.
* Up and Down arrows navigate suggestions.
* The mouse can hover and select suggestions.
* Enter selects and submits the active suggestion.
* Tab fills the active autocomplete suggestion and immediately submits it.
* Escape closes the suggestion list.
* Tab must continue normal focus navigation when there is no active suggestion.
* Prevent accidental double submission.
* Support empty data and no-match states.
* Preserve visible keyboard focus.
* Use sensible ARIA labels and keyboard-accessible markup.

Use one shared implementation for all fuzzy-search fields, including application autocomplete later in this task.

## Feature 2: Full-screen instructor messages

Improve instructor messages displayed on student computers.

Support at least these message types:

* Informational message
* Student warning
* Transition-time message

Requirements:

* Display messages in a true full-screen student overlay.
* Cover the full usable screen.
* Keep the message above ordinary application windows using documented Windows APIs.
* Clearly identify the overlay as an iD Tech classroom message.
* Do not disguise it as a system warning.

For student warnings and transition-time messages:

* Do not provide a student dismiss button.
* Keep the overlay active until:

  * The instructor disables it, or
  * Its configured timeout expires
* If the overlay window is accidentally closed or crashes while the message remains active on the server, recreate it.
* Do not recreate it after the instructor has disabled it.
* Reconnection must query authoritative server state so stale messages do not return.
* Do not make the process hidden or administrator-unkillable.

For informational messages:

* Preserve normal dismissal behavior if that matches the current design.

Where reasonably compatible with the current project:

* Display enforced messages across all active monitors.
* Handle monitor connection and removal.
* Keep message text legible and properly scaled.
* Prevent HTML or script injection in message content.

## Feature 3: Message timeout

Add an optional timeout to instructor messages.

Requirements:

* Allow no timeout or a custom timeout.
* Make the time unit explicit.
* Validate values in both the browser interface and server.
* Reject malformed, negative, zero, or excessive timeout values.
* Prefer storing or transmitting an absolute expiration timestamp so reconnecting does not restart the timer.
* When the timeout expires:

  * Remove the message from the student screen
  * Update authoritative server state
  * Update the instructor interface
* Cancel any pending local timer when the instructor manually disables the message.
* Prevent expired messages from reappearing after reconnect.

## Feature 4: Close the currently focused student window

Add an instructor action to request that a selected student computer close its current focused site, browser window, or foreground application.

Requirements:

* Use the existing command channel between server and student agent.
* Enforce authorization on the server.
* Do not rely only on hiding the button in the website.
* Label the action clearly.
* Add confirmation where appropriate because unsaved student work may be affected.
* Prefer a graceful close request to the foreground window.
* Do not kill random processes based on unreliable name matching.
* Do not terminate unrelated background applications.
* Return a result to the instructor:

  * Success
  * No foreground window
  * Unsupported
  * Timed out
  * Failed
* Record the action in the existing log system, or add a lightweight log if none exists.

If the current client can reliably close an active managed browser tab rather than an entire application window, use the least destructive reliable option.

## Feature 5: Custom “block for” duration

Improve the existing “block for” control.

Requirements:

* Add a custom duration input.
* Default to 1 minute.
* Keep any current preset options.
* Clearly label the time unit.
* Validate values on both client and server.
* Reject zero, negative, malformed, or excessive values (longer than 20hrs).
* Use an absolute expiration time where possible.
* Ensure reconnecting does not restart or extend a block.
* Keep the instructor countdown and server state consistent.
* Support manual cancellation.

## Feature 6: Class-level custom application blocking

Add a section where an authorized instructor or director can define applications that should be disabled for a class.

Before implementing, inspect how classes, rooms, locations, and sessions are represented. Use the existing representation rather than inventing an unnecessary database or framework.

Requirements:

* Rules must be scoped to the correct class, room, session, or location entity used by the project.
* Allow authorized users to add and remove application rules.
* Persist the rules using the project’s existing storage method.
* If the application currently stores state in JSON or local files, continue that pattern unless it is unsafe.
* Validate all changes server-side.
* Use a stable executable or process identifier where available.
* Store a readable display name separately.
* Avoid broad substring matching that could block unrelated software.
* Clearly show current blocked applications.
* Display errors instead of failing silently.
* Maintain compatibility with existing saved data.

## Feature 7: Open-application inventory

Have each connected student agent report a limited inventory of applications currently open.

Collect only metadata needed for classroom management:

* Process or executable name
* Human-readable application name when available
* Stable executable identifier or path where appropriate
* Timestamp of the last report

Do not collect:

* Keystrokes
* Passwords
* Window text or document contents
* Browser history
* Chat contents
* Private file contents

Requirements:

* Use the existing authenticated client/server connection.
* Deduplicate repeated processes into useful application choices.
* Mark stale inventory clearly.
* Handle disconnected computers.
* Sanitize values before displaying them in HTML.
* Do not trust client-provided data for authorization.

Use the open-application inventory as autocomplete options when adding a blocked application.

The application autocomplete must use the same local fuzzy-search implementation:

* Arrow-key navigation
* Mouse navigation
* Enter to select
* Tab to fill the active suggestion and submit
* Escape to close
* Stable ranking

If no detected application matches the entered value:

* Allow the authorized user to add a custom application name or executable identifier.
* Clearly distinguish manually entered rules from currently detected applications.

## Feature 8: Windows client packaging

Complete the current partially implemented Windows executable and launcher/watchdog work.

First inspect:

* `app.cs`
* Existing PowerShell scripts
* Existing compile commands
* Existing generated files
* README packaging instructions
* Any partial executable-download implementation

Do not replace an existing viable packaging method without a concrete reason.

Target result:

* A reproducible build that produces an executable named:

  `iD-Tech-Watch.exe`

* A documented build command

* A documented output path

* No fabricated binary if the environment cannot compile it

### Runtime installation path

Do not hard-code `C:\Users\Student`.

Use the current Windows user’s Documents folder.

Resolve the Documents folder through a supported Windows or .NET API rather than assuming it is always at a literal path.

Create this directory structure:

`<CurrentUserDocuments>\projects\iD-Tech\`

For example, it may resolve to:

`C:\Users\<CurrentUser>\Documents\projects\iD-Tech\`

Create the directories if they do not exist.

The installed runtime files should include:

* The primary classroom-management agent
* A visible watchdog or launcher that monitors the primary agent

Both components must use clear iD Tech naming.

### Watchdog behavior

The watchdog may:

* Detect an accidental agent close or crash
* Restart the agent
* Use restart backoff
* Prevent duplicate instances
* Start the agent after login using documented Windows mechanisms, if that is already part of the project

The watchdog must not:

* Hide from Task Manager
* Continuously fight an explicit administrative stop
* Relaunch during uninstall
* Create an uncontrolled process loop
* Bypass Windows security tools
* Require administrator privileges for ordinary per-user installation

Implement a reliable authorized shutdown flow.

Possible approaches include:

* A shared shutdown marker
* A named event
* A local IPC command
* A shutdown file stored in the application data directory
* Another simple mechanism consistent with the existing code

During authorized shutdown:

* Both the watchdog and agent should stop.
* Neither should restart the other.
* Normal restart behavior should resume at the next intended launch unless the software has been disabled or uninstalled.

Do not use an undocumented student-accessible secret key combination as the primary shutdown method.

If the existing project already has a developer shutdown shortcut, it may remain only if it is documented, safe, and not the sole administrative control.

### Packaging requirements

* Prefer a per-user installation that does not require administrator privileges.
* Quote Windows paths safely.
* Handle spaces in Documents paths and usernames.
* Avoid shell-command injection.
* Prevent duplicate agent instances.
* Handle an absent installation folder.
* Handle partially installed files.
* Handle replacement of an older version where practical.
* Include bounded logging.
* Use clear executable metadata where the current build method supports it.
* Do not commit build output unless the repository already treats binaries as release assets.

If the current environment cannot produce the Windows executable:

* Complete all source code.
* Complete all PowerShell or compiler scripts.
* Validate their syntax where possible.
* Give the exact Windows command needed to build it.
* State the expected output filename and location.
* Do not claim that the executable was generated.

## Feature 9: Authenticated website download

Add a website button for downloading `iD-Tech-Watch.exe`.

Requirements:

* Show it after the user passes the first login screen.

* Allow the authenticated instructor and director roles.

* Enforce download permissions on the server.

* Do not rely only on the browser UI.

* Place the button in the bottom-right area.

* Style it similarly to the existing monitor button.

* Use a clear label such as:

  `Download iD-Tech-Watch`

* Serve the file through a safe fixed route.

* Do not accept an arbitrary filesystem path from the browser.

* Use an appropriate filename and content type.

* Return a useful error if the executable has not been built.

* Preserve responsive layout and keyboard accessibility.

## Implementation quality

Work within the existing small architecture.

Do not introduce React, Electron, TypeScript, Express, a database, npm, or another major dependency unless the project already uses it.

Prefer:

* Small reusable JavaScript functions
* Existing plain HTML, CSS, and JavaScript patterns
* Existing Node APIs
* Existing C# code
* Existing PowerShell tooling
* Minimal targeted changes
* Clear separation between browser code, server code, student agent code, and Windows launcher code

Security requirements:

* Validate every remote command.
* Enforce authorization server-side.
* Escape all untrusted HTML.
* Prevent command injection.
* Prevent path traversal.
* Avoid unsafe `eval`, shell interpolation, or dynamically constructed PowerShell commands.
* Clean up timers, sockets, event handlers, and child processes.
* Avoid exposing student application inventory to unauthorized users.
* Maintain compatibility with existing clients where practical.
* Handle older or missing command fields defensively.

Do not leave core requested features as:

* TODO comments
* Empty handlers
* Mock buttons
* Console-only simulations
* Pseudocode
* Unconnected helper functions
* UI elements with no backend implementation

## Testing and verification

Use the project’s actual available tooling. Since this repository may not use npm, do not assume `npm test` or `npm run build` exists.

Inspect the README and scripts first.

Run all relevant checks that actually exist, such as:

* JavaScript syntax checks using `node --check`
* Existing test scripts
* Existing PowerShell validation or build scripts
* C# compilation
* Server startup smoke testing
* Agent startup smoke testing
* HTTP route testing
* WebSocket or socket command testing
* Manual browser-interface verification where automated browser tooling is unavailable

At minimum, validate:

* The server starts without syntax errors.
* The website loads.
* Existing login behavior still works.
* Location search ranks matches correctly.
* Unmatched tiles remain visible and clickable.
* Tab autocomplete selects and submits.
* Application autocomplete supports detected and custom entries.
* Message timeouts expire correctly.
* Instructor-disabled messages do not reappear.
* Block durations default to one minute.
* Custom block durations are validated.
* Foreground-window close commands return a result.
* Application rules persist and remain class-scoped.
* The agent reports open applications.
* The watchdog restarts the agent after an accidental close.
* Authorized shutdown prevents both processes from restarting.
* The download route rejects unauthenticated users.
* Instructor and director accounts can download the executable when it exists.
* The download route handles a missing binary safely.
* Runtime paths resolve through the active user’s Documents folder.

Add small focused test scripts where the project has no formal test framework. Do not add a large test dependency.

Do not suppress failures or remove existing functionality merely to get a passing result.

## Completion report

When finished, provide:

1. A summary of the implemented features.
2. A file-by-file summary of meaningful changes.
3. A clear explanation of how the local fuzzy-search ranking works.
4. The exact tests and commands run.
5. The result of each command.
6. Any Windows-specific behavior that could not be executed in the current environment.
7. The exact command to build `iD-Tech-Watch.exe`.
8. The expected executable output path.
9. The runtime installation path beneath the current user’s Documents folder.
10. The administrative shutdown procedure.
11. Known limitations that still genuinely remain.
12. A manual acceptance checklist for every requested feature.
13. Keep a thorough log of your process in /docs/07-22-26--10-03pm-output.md

Do not stop after reading the repository or producing a plan. Continue implementing until the requested work and all validation possible in the current environment are complete. Take it in reasonable steps, not all at once. Reset/compress context windows when it makes sense and you are at a good reset point, not when it just gets to 100%.